version: '3.8'

services:
  travel-agent:
    build: .
    container_name: tutu-travel-agent
    restart: unless-stopped
    ports:
      - "8001:8000"   # слева порт на хосте, справа — внутри контейнера
    env_file:
      - .env
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
